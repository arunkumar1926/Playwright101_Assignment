{
  "name": "playwright101_assignment",
  "version": "1.0.0",
  "author": "Arun Kumar K",
  "type": "module",
  "engines": {
    "node": ">=14"
  },
  "description": "Playwright assignment project to run the tests using playwright runner on LambdaTest platform",
  "scripts": {
    "test": "npx playwright test --config=./playwright.config.ts"
  },  
  "devDependencies": {
    "@playwright/test": "1.29.2",
    "playwright": "1.29.2"
  }
}